{
	"name": "My OpenCode Project",
	"build": {
		"context": "..",
		"dockerfile": "dockerfile"
	},
	"capAdd": [
		"NET_ADMIN",
		"NET_RAW"
	],
	"remoteUser": "node",
	// "postStartCommand": "sudo /usr/local/bin/init-firewall.sh",
	"features": {
		"ghcr.io/devcontainers-extra/features/uv:1": {}
	},
	"forwardPorts": [
		5173,
		8080
	],
	"postCreateCommand": "git submodule update --init && npm install -g opencode-ai@latest && npm install -g pnpm && pnpm install && julia --project=server -e 'using Pkg; Pkg.instantiate(); Pkg.precompile()'",
	"customizations": {
		"vscode": {
			"extensions": [
				"sst-dev.opencode",
				"svelte.svelte-vscode",
				"julialang.language-julia",
				"tamasfe.even-better-toml"
			]
		}
	}
}